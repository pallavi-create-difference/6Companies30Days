vector<string> generate(int N)
{
	vector<string> ans;
	for(int i=1;i<=N;i++){
	    int n=i;
	    string temp="";
	    while(n>0){
	       int t=n%2;
	       temp=to_string(t)+temp;
	       n=n/2;
	    }
	    ans.push_back(temp);
	}
	return ans;
	
}
